---
description: Quick alias for error tracking (invokes /track-errors)
---

# Fail Tracking (Alias)

This is a convenience alias for `/track-errors`. Use `/fail` when you want to quickly log an error or issue.

**Auto-invoke:** Now loading `/track-errors` command...
